# create-murphy-backend 🏗️💼

A powerful backend scaffolding tool by **MoneyKingMurph** 👑

Quickly spin up a modern Express + MongoDB project with just one command:

```bash
npx create-murphy-backend directoryName